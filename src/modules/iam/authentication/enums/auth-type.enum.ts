/**
 * Enum representing the types of authentication that can be used in the application.
 */
export enum AuthType {
  /**
   * Bearer token authentication.
   */
  Bearer = 'Bearer',

  /**
   * No authentication required.
   */
  None = 'None',
}
